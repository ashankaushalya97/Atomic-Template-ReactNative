import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginLeft: 10,
    marginRight: 10,
    marginTop: 20,
    borderRadius: 5,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 15,
    backgroundColor: '#3898FF',
  },
  buttonText: {color: '#ffff', fontWeight: '600'},
});

export default styles;
